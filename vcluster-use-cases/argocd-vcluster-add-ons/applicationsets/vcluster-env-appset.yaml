apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vcluster-env-config
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - clusters:
        selector:
          matchLabels:
            env: dev
    - clusters:
        selector:
          matchLabels:
            env: qa
    - clusters:
        selector:
          matchLabels:
            env: prod
  template:
    metadata:
      name: rmweir-auto-nodes-app-{{.name}}
    spec:
      destination:
        namespace: rmweir-auto-nodes-app-{{.metadata.labels.env}}
        name: '{{.name}}'
      info:
        - name: Git Repo
          value: 'https://github.com/loft-demos/rmweir-auto-nodes-app/'
      project: 'loft-{{ index .metadata.labels "demos.vcluster.com/project" }}'
      source:
        path: vcluster-use-cases/argocd-vcluster-add-ons/manifests/{{.metadata.labels.env}}/
        repoURL: 'https://github.com/loft-demos/rmweir-auto-nodes-app.git'
        targetRevision: 'main'
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
